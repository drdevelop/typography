{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "typography",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "cxd",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
